# Database seeding

---

To get started on this project with some test data in the artists area.

**Get the latest database migrations:**

```bash
php artisan migrate
```

**Add 50 artists to the database:**

You can run this command multiple times if you wish to add another 50.

```bash
php artisan db:seed
```
